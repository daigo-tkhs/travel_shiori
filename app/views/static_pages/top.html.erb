<%# ヘッダーとフッターを非表示 %>
<% @hide_header = true %>
<% @hide_footer = true %>

<%# --- 背景全体 --- %>
<div class="min-h-screen w-full relative bg-gradient-to-br from-purple-600 via-blue-600 to-blue-500 text-white font-sans overflow-x-hidden">

  <%# --- レイアウトコンテナ --- %>
  <div class="relative z-10 min-h-screen flex flex-col px-6 py-10">

    <%# メインコンテンツエリア %>
    <div class="flex-1 flex flex-col items-center justify-center w-full max-w-5xl mx-auto text-center">
      
      <%# ■ 上ブロック: タイトルと説明文 %>
      <div class="flex flex-col items-center animate-fade-in-down w-full">
        
        <%# アプリ名 (サイズ固定: スマホ〜PCで可変) %>
        <h1 class="font-extrabold tracking-tight drop-shadow-xl mb-6" style="font-size: clamp(3.5rem, 10vw, 7rem); line-height: 1.1;">
          TripConcierge
        </h1>
        
        <%# 
            ★修正: 説明文の下に強力なマージンを強制適用
            style="margin-bottom: 5rem;" (約80px)
            これでボタンとの間に十分な空間が生まれます。
        %>
        <p class="text-lg md:text-2xl text-blue-50 font-medium max-w-2xl leading-relaxed" style="margin-bottom: 5rem;">
          AIと一緒に、最高の思い出を計画しよう。<br>
          あなただけの「旅のしおり」を、もっと自由に。
        </p>
      </div>

      <%# ■ 下ブロック: アクションボタン %>
      <div class="w-full max-w-md animate-fade-in-up delay-200">
        
        <%# ボタンエリア (上下の隙間 space-y-6 を維持) %>
        <div class="flex flex-col space-y-6 sm:space-y-0 sm:flex-row sm:gap-6">
          
          <%# 1. 新規登録ボタン %>
          <%= link_to new_user_registration_path, class: "group w-full flex items-center justify-center gap-2 border-2 border-white bg-transparent text-white font-bold py-4 px-8 rounded-full shadow-2xl transition-all duration-300 hover:bg-white/20 hover:scale-105 active:scale-95" do %>
            <span>無料で始める</span>
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor" class="w-5 h-5 transition-transform group-hover:translate-x-1">
              <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5 21 12m0 0-7.5 7.5M21 12H3" />
            </svg>
          <% end %>

          <%# 2. ログインボタン %>
          <%= link_to new_user_session_path, class: "group w-full flex items-center justify-center gap-2 border-2 border-white bg-transparent text-white font-bold py-4 px-8 rounded-full shadow-2xl transition-all duration-300 hover:bg-white/20 hover:scale-105 active:scale-95" do %>
            <span>ログイン</span>
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor" class="w-5 h-5 transition-transform group-hover:translate-x-1">
              <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 9V5.25A2.25 2.25 0 0 0 13.5 3h-6a2.25 2.25 0 0 0-2.25 2.25v13.5A2.25 2.25 0 0 0 7.5 21h6a2.25 2.25 0 0 0 2.25-2.25V15M12 9l-3 3m0 0 3 3m-3-3h12.75" />
            </svg>
          <% end %>
        </div>
      </div>

    </div>

    <%# フッター %>
    <div class="w-full text-center mt-12 text-white/40 text-xs">
      &copy; <%= Time.current.year %> TripConcierge
    </div>

  </div>
</div>

<style>
  @keyframes fadeInDown { from { opacity: 0; transform: translateY(-20px); } to { opacity: 1; transform: translateY(0); } }
  @keyframes fadeInUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
  .animate-fade-in-down { animation: fadeInDown 0.8s ease-out forwards; }
  .animate-fade-in-up { animation: fadeInUp 0.8s ease-out forwards; opacity: 0; animation-fill-mode: forwards; }
  .delay-200 { animation-delay: 0.2s; }
</style>