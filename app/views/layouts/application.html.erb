<!DOCTYPE html>
<html>
  <head>
    <title>TravelShiori</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%# Tailwind CSSの読み込み %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    
    <%= javascript_importmap_tags %>
  </head>

  <%# 全体の背景を薄いグレーにし、アプリの雰囲気を出す %>
  <body class="bg-gray-100 text-gray-800 font-sans antialiased">
    
    <%# スマホサイズ（最大幅md: 約450px）に制限し、中央寄せ + 影をつける %>
    <div class="max-w-md mx-auto min-h-screen bg-white shadow-2xl relative pb-24">
      
      <%# 共通ヘッダー（詳細画面など @hide_header が true の時は表示しない） %>
      <%= render 'shared/header' unless @hide_header %>
      
      <%# フラッシュメッセージ（通知）の表示エリア %>
      <% if notice %>
        <div class="bg-blue-100 border-l-4 border-blue-500 text-blue-700 p-4 m-4 rounded shadow-sm" role="alert">
          <p><%= notice %></p>
        </div>
      <% end %>
      <% if alert %>
        <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 m-4 rounded shadow-sm" role="alert">
          <p><%= alert %></p>
        </div>
      <% end %>

      <%# メインコンテンツ %>
      <main class="p-4">
        <%= yield %>
      </main>

      <%= render 'shared/footer' %>
    </div>
  </body>
</html>