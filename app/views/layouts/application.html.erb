<!DOCTYPE html>
<html>
  <head>
    <title>travel_shiori</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= javascript_importmap_tags %>

    <% if Rails.env.production? %>
      <script type="module" src="<%= asset_path 'controllers/map_controller.js' %>"></script>
      <script type="module" src="<%= asset_path 'controllers/sortable_controller.js' %>"></script>
      <script type="module" src="<%= asset_path 'controllers/geocoding_controller.js' %>"></script>
    <% end %>
    
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  </head>

  <%# body自体を画面いっぱいのコンテナにする %>
  <body class="bg-gray-50 text-gray-800 font-sans antialiased min-h-screen flex flex-col">
    
    <%# 1. ヘッダーを最上位に配置して w-full を活かす %>
    <%= render 'shared/header' unless @hide_header %>

    <%# --- メインコンテンツエリア --- %>
    <div class="flex-1 w-full mx-auto relative flex flex-col">
      
      <div id="flash_container">
        <% if notice %>
          <div class="max-w-7xl mx-auto w-full px-4 mt-4">
            <div class="bg-blue-50 border-l-4 border-blue-500 text-blue-700 p-4 rounded shadow-sm text-sm" role="alert">
              <p><%= notice %></p>
            </div>
          </div>
        <% end %>
        <%# (alertも同様に max-w-7xl mx-auto で囲むと綺麗です) %>
      </div>

      <%# メインコンテンツ %>
      <%# 
          ★重要: trips/new などで max-w-4xl mx-auto を指定しているので、
          ここでは余計な幅制限をかけず yield させます。
      %>
      <main class="flex-1 w-full">
        <%= yield %>
      </main>

      <%# フッター %>
      <% unless @hide_footer %>
        <%= render "shared/footer" %>
      <% end %>
    </div>
  </body>
</html>