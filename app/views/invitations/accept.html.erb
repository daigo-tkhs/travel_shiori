<%# ▼▼▼ 1. 背景レイヤー: 画面に固定(fixed)して絶対に動かないようにします ▼▼▼ %>
<div class="fixed inset-0 bg-gradient-to-br from-purple-600 to-blue-500 z-0"></div>

<%# ▼▼▼ 2. コンテンツレイヤー: 背景の上に重ねて(relative z-10)、スクロール可能にします ▼▼▼ %>
<div class="relative z-10 min-h-screen flex flex-col justify-center py-12 sm:px-6 lg:px-8">
  
  <%# ロゴとアプリ名 %>
  <div class="sm:mx-auto sm:w-full sm:max-w-md text-center mb-6">
    <div class="flex justify-center items-center gap-3 mb-2">
      <% if Rails.application.assets.present? && Rails.application.assets.find_asset('logo.png').present? %>
        <%= image_tag "logo.png", class: "h-14 w-auto object-contain", alt: "TripConcierge Logo" %>
      <% else %>
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-12 h-12 text-white">
          <path stroke-linecap="round" stroke-linejoin="round" d="M6 12 3.269 3.125A59.769 59.769 0 0 1 21.485 12 59.768 59.768 0 0 1 3.27 20.875L5.999 12Zm0 0h7.5" />
        </svg>
      <% end %>
      <span class="text-3xl font-bold text-white tracking-tight">TripConcierge</span>
    </div>
  </div>

  <%# タイトルエリア %>
  <div class="sm:mx-auto sm:w-full sm:max-w-md">
    <h2 class="mt-2 text-center text-3xl font-extrabold text-white">
      招待を受け取る
    </h2>
    <p class="mt-2 text-center text-sm text-gray-100">
      以下の旅程への招待が届いています
    </p>
  </div>

  <%# カード部分 %>
  <div class="mt-8 sm:mx-auto sm:w-full sm:max-w-md">
    <div class="bg-white py-8 px-4 shadow-xl sm:rounded-lg sm:px-10">
      
      <%# 旅程情報 %>
      <div class="bg-blue-50 border border-blue-100 rounded-lg p-4 mb-6 text-center">
        <h3 class="text-lg font-bold text-gray-800"><%= @invitation.trip.title %></h3>
        <p class="text-sm text-gray-500 mt-1">
          権限: <span class="font-bold"><%= @invitation.role == 'editor' ? '編集者' : '閲覧者' %></span>
        </p>
      </div>

      <div class="space-y-4">
        <%# 1. ログインして参加 %>
        <%= link_to new_user_session_path(invitation_token: @invitation.token), class: "w-full flex justify-center py-3 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition" do %>
          ログイン / 新規登録して参加
        <% end %>

        <%# 2. ゲスト閲覧ボタン %>
        <% if @invitation.role == 'viewer' %>
          <div class="relative py-2">
            <div class="absolute inset-0 flex items-center">
              <div class="w-full border-t border-gray-300"></div>
            </div>
            <div class="relative flex justify-center text-sm">
              <span class="px-2 bg-white text-gray-500">または</span>
            </div>
          </div>
          
          <%= button_to accept_guest_invitation_path(token: @invitation.token), method: :post, class: "..." do %>
            登録せずにゲストとして閲覧
          <% end %>
          
          <p class="text-xs text-center text-gray-400 mt-2">
            ※ゲスト閲覧はブラウザを閉じると終了します
          </p>
        <% else %>
          <p class="mt-4 text-xs text-center text-yellow-600 bg-yellow-50 p-2 rounded">
            ※編集権限での参加にはアカウント登録が必要です。
          </p>
        <% end %>
      </div>

    </div>
  </div>
</div>