<div class="min-h-screen bg-gray-50 pt-24 pb-12">
  <div class="max-w-2xl mx-auto px-6">
    
    <div class="bg-white rounded-3xl shadow-xl border border-gray-100 overflow-hidden">
      <%# ヘッダー背景エリア %>
      <div class="h-32 bg-gradient-to-r from-purple-600 to-blue-600"></div>

      <div class="px-8 pb-10 relative">
        <%# アイコン（アバター） %>
        <div class="relative -mt-16 mb-6 flex justify-center">
          <div class="w-32 h-32 rounded-full border-4 border-white bg-gray-200 flex items-center justify-center shadow-md overflow-hidden">
            <%# 画像があれば表示、なければイニシャルを表示 %>
            <span class="text-4xl font-bold text-gray-400">
              <%= @user.nickname.present? ? @user.nickname[0].upcase : "U" %>
            </span>
          </div>
        </div>

        <%# ユーザー情報 %>
        <div class="text-center mb-8">
          <h1 class="text-3xl font-bold text-gray-900 mb-2">
            <%= @user.nickname.presence || "名無しさん" %>
          </h1>
          <p class="text-gray-500 text-sm">
            <%= @user.email %>
          </p>
          <p class="text-gray-400 text-xs mt-2">
            <%= @user.created_at.strftime("%Y年%m月%d日") %> から利用中
          </p>
        </div>

        <%# アクションボタン %>
        <div class="flex flex-col gap-4 max-w-sm mx-auto">
          <%# 本人だけが編集ボタンを見れるように制御 %>
          <% if current_user == @user %>
            <%= link_to edit_user_registration_path, class: "flex items-center justify-center gap-2 w-full py-3 px-6 rounded-full bg-blue-600 hover:bg-blue-700 text-white font-bold transition-all shadow-md active:scale-95" do %>
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" class="w-5 h-5">
                <path d="m5.433 13.917 1.262-3.155A4 4 0 0 1 7.58 9.42l6.92-6.918a2.121 2.121 0 0 1 3 3l-6.92 6.918c-.383.383-.84.685-1.343.886l-3.154 1.262a.5.5 0 0 1-.65-.65Z" />
                <path d="M3.5 5.75c0-.69.56-1.25 1.25-1.25H10A.75.75 0 0 0 10 3H4.75A2.75 2.75 0 0 0 2 5.75v9.5A2.75 2.75 0 0 0 4.75 18h9.5A2.75 2.75 0 0 0 17 15.25V10a.75.75 0 0 0-1.5 0v5.25c0 .69-.56 1.25-1.25 1.25h-9.5c-.69 0-1.25-.56-1.25-1.25v-9.5Z" />
              </svg>
              プロフィールを編集する
            <% end %>
          <% end %>

          <%= link_to :back, class: "block text-center text-sm font-bold text-gray-400 hover:text-gray-600 mt-2 transition" do %>
            戻る
          <% end %>
        </div>

      </div>
    </div>

  </div>
</div>